# Reproduce the failing class of zpsu_mon WiFi builds: full newlib (not nano)
# plus Zephyr's POSIX sysconf() function implementation.
CONFIG_NEWLIB_LIBC=y
CONFIG_POSIX_API=y
CONFIG_POSIX_SYSCONF_IMPL_FULL=y
